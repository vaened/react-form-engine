# React Form Engine

Composable, scope-first form engine for React.

This project is focused on:

- Familiar developer experience for React forms
- A different core model built around composition and scope
- Better support for partial updates and nested structures
